{
  "name": "@jupiterone/npm-enforce-policy",
  "version": "1.1.0",
  "description": "Scan a user's NPM tokens and profile and fail with exit code 1 if any fail to comply with policy.",
  "repository": "https://github.com/JupiterOne/npm-enforce-policy",
  "directories": {
    "src": "./src",
    "bin": "./bin"
  },
  "bin": {
    "npm-enforce-policy": "./bin/npm-enforce-policy"
  },
  "scripts": {
    "pretest": "yarn lint",
    "test": "jest --silent",
    "lint": "eslint --ignore-path .gitignore ."
  },
  "license": "MPL-2.0",
  "devDependencies": {
    "eslint": "^7.20.0",
    "jest": "^26.6.3"
  }
}

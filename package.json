{
  "name": "@jupiterone/npm-enforce-token-policy",
  "version": "1.0.0",
  "description": "Scan a user's NPM tokens and fail with exit code 1 if any fail to comply with policy.",
  "repository": "https://github.com/JupiterOne/npm-enforce-token-policy",
  "directories": {
    "src": "./src",
    "bin": "./bin"
  },
  "bin": {
    "npm-enforce-token-policy": "./bin/npm-enforce-token-policy"
  },
  "scripts": {
    "pretest": "yarn lint",
    "test": "jest --silent",
    "lint": "eslint --ignore-path .gitignore ."
  },
  "license": "MPL-2.0",
  "devDependencies": {
    "eslint": "^7.20.0",
    "jest": "^26.6.3"
  }
}
